import BehaviorSubject from './BehaviorSubject';

export default class PropertyPathSubject extends BehaviorSubject {
  constructor(defaultValue) {
    super(defaultValue);
  }

  getPropertyPath(path) {

  }
}